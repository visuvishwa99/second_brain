I'd be happy to create a comprehensive set of flashcards on Apache Spark performance optimization techniques. Based on the information provided, I'll create flashcards covering key concepts and best practices for improving Spark performance.

# Apache Spark Performance Optimization Flashcards

- What is the default storage level when using cache() on an RDD? :-> {{ MEMORY_ONLY }}

- What is the default storage level when using cache() on a Dataset? :-> {{ MEMORY_AND_DISK }}

- When should you use persist() instead of cache()? :-> {{ When you want to assign a storage level other than the default (MEMORY_ONLY for RDD or MEMORY_AND_DISK for Dataset) }}

- List the five storage levels available with persist() :-> {{ MEMORY_ONLY, MEMORY_AND_DISK, MEMORY_ONLY_SER, MEMORY_AND_DISK_SER, DISK_ONLY }}

- What does MEMORY_ONLY_SER storage level mean? :-> {{ Data is stored in serialized format in memory only }}

- What is Kryo and why is it better than Java's default serializer? :-> {{ Kryo is a serialization library that converts objects into a compact binary format much faster than Java's default serialization }}

- Which Spark APIs are generally recommended over RDDs and why? :-> {{ DataFrames and Datasets, because they leverage Spark's Catalyst optimizer and offer better performance }}

- What is the primary benefit of caching and persisting data in Spark? :-> {{ Reduces disk I/O and recomputation overhead by keeping frequently used data in memory }}

- What is a broadcast table in Spark? :-> {{ A read-only dataset that is distributed to all worker nodes in advance to avoid shuffling during joins }}

- When should you use broadcast tables? :-> {{ For smaller tables that need to be accessed by multiple tasks, especially in join operations }}

- What is Predicate Pushdown in Spark SQL? :-> {{ A technique that improves query performance by pushing down filters to the data source, reducing the amount of data that needs to be processed }}

- Name three join strategies in Spark :-> {{ Broadcast hash join, shuffle hash join, sort-merge join }}

- When is a broadcast hash join preferred? :-> {{ When one of the tables is small enough to fit in memory and can be broadcast to all worker nodes }}

- What is partitioning in Spark and why is it important? :-> {{ Dividing data across multiple nodes to enable parallel processing and optimize join operations }}

- What is Hive bucketing and when is it useful? :-> {{ A technique that organizes data into buckets based on hash values of specified columns, useful for optimizing join operations on large datasets }}

- What is Z-order clustering? :-> {{ A multi-dimensional clustering technique that improves data locality for range queries on multiple columns }}

- What configuration should you adjust to allocate memory for caching in Spark? :-> {{ spark.memory.fraction and spark.memory.storageFraction }}

- What garbage collector is recommended for Spark applications? :-> {{ G1GC (Garbage-First Garbage Collector) }}

- How do you configure G1GC for a Spark application? :-> {{ Using spark.driver.extraJavaOptions with flags like -XX:+UseG1GC, -XX:G1HeapRegionSize=n, and -XX:MaxGCPauseMillis=m }}

- A testing utility for comparing DataFrames, introduced in Apache Spark 3.5 assertDataFrameEqual ? :-> {{ df.assertDataFrameEqual }}

- For verifying that two DataFrame schemas match during testing used for? :-> {{ df.assertSchemaEqual }}

- What happens if you don't properly unpersist() cached RDDs? :-> {{ Memory leaks can occur, reducing available memory for other operations }}

- What is the difference between repartition() and coalesce()? :-> {{ repartition() shuffles all data and creates equal-sized partitions, while coalesce() minimizes shuffling by reducing partitions but may create uneven partitions }}

- When performing aggregations, what technique can improve performance? :-> {{ Using pre-aggregation steps to reduce data volume before final aggregation }}

- What is the benefit of the Tungsten execution engine in Spark? :-> {{ It provides optimized memory management and code generation for better CPU efficiency }}

- Why is controlling the number of partitions important? :-> {{ Too few partitions limits parallelism, while too many increases task scheduling overhead }}

- What is data skew and how can it impact Spark performance? :-> {{ Uneven distribution of data across partitions, causing some tasks to take much longer than others }}

- How can you mitigate data skew in Spark? :-> {{ By using salting techniques, custom partitioning, or broadcasting smaller datasets }}

- What is the benefit of using parquet file format in Spark? :-> {{ Columnar storage provides better compression and enables column pruning and predicate pushdown }}

# Databricks Performance Optimization Flashcards

I'll create a comprehensive set of flashcards on Databricks optimization techniques based on the table you've shared. Here are the flashcards in the requested format:

- What syntax is used for table partitioning in Databricks? :-> {{ PARTITION BY order_date }}

- What is the primary benefit of partitioning in Databricks? :-> {{ Queries only scan relevant partitions, reducing the amount of data processed }}

- What syntax is used for clustering in Databricks? :-> {{ CLUSTERED BY (store_id) INTO 10 BUCKETS }}

- How does clustering improve performance in Databricks? :-> {{ Provides faster lookups within partitions by organizing similar data together }}

- What is Z-Order indexing in Databricks and how is it implemented? :-> {{ A multi-dimensional clustering technique implemented with OPTIMIZE ... ZORDER BY (column) }}

- What type of queries benefit most from Z-Order indexing? :-> {{ Queries with WHERE filters on the Z-ordered columns }}

- What command is used for file compaction in Databricks? :-> {{ OPTIMIZE table_name }}

- What performance benefit does file compaction provide? :-> {{ Reduces small files and improves query speed by creating more efficient file layouts }}

- What syntax is used for storage cleanup in Databricks? :-> {{ VACUUM table_name RETAIN X HOURS }}

- What is the purpose of the RETAIN parameter in VACUUM commands? :-> {{ Specifies how long to keep older versions of the data before cleaning them up }}

- How do you cache a table in Spark within Databricks? :-> {{ CACHE TABLE dim_store }}

- What performance benefit does table caching provide? :-> {{ Avoids re-reading data from storage by keeping frequently accessed data in memory }}

- How do you enable the Databricks IO Cache? :-> {{ SET spark.databricks.io.cache.enabled = true; }}

- What is the benefit of the Databricks IO Cache compared to Spark Cache? :-> {{ Reduces cloud storage reads by caching at the I/O layer, benefiting all queries }}

- How do you enable Adaptive Query Execution in Databricks? :-> {{ SET spark.sql.adaptive.enabled = true; }}

- What does Adaptive Query Execution optimize? :-> {{ Auto-optimizes shuffle partitions based on actual data sizes during query execution }}

- What syntax indicates a broadcast join in Databricks? :-> {{ /*+ BROADCAST(dim_customer) */ }}

- When should broadcast joins be used? :-> {{ When joining a large fact table with a smaller dimension table that can fit in memory }}

- What is Cluster Autoscaling in Databricks? :-> {{ A feature that automatically adjusts the number of worker nodes based on workload }}

- What type of workloads benefit most from Cluster Autoscaling? :-> {{ Variable workloads like dynamic ETL processing that have fluctuating resource requirements }}

- What query optimization technique reduces unnecessary processing? :-> {{ Filter early in the query and use Window Functions for efficient calculations }}

- Which tables are typically good candidates for caching? :-> {{ Dimension tables (dim tables) that are frequently joined with fact tables }}

- What is the difference between partitioning and clustering? :-> {{ Partitioning divides data into separate directories while clustering organizes data within partitions }}

- How does VACUUM improve metadata management? :-> {{ Frees up storage and improves performance by removing old file versions and orphaned files }}

- What is a common use case for Z-Order indexing? :-> {{ Queries that filter on multiple columns, such as store_id and date in a fact_sales table }}

- Why is the BROADCAST hint useful for dimension tables? :-> {{ It avoids shuffling by distributing the smaller dimension table to all nodes }}

- What happens if you don't periodically run OPTIMIZE on Delta tables? :-> {{ Small files accumulate, degrading query performance over time }}

- What does the "INTO 10 BUCKETS" part of clustering syntax control? :-> {{ The number of files data will be organized into within each partition }}

- How should you determine the optimal number of buckets for clustering? :-> {{ Based on the cardinality of the clustering column and desired file sizes }}

- What's a key advantage of using Delta Lake format in Databricks? :-> {{ It supports ACID transactions and enables optimization commands like OPTIMIZE and VACUUM }}