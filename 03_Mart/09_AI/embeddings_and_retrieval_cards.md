TARGET DECK: SecondBrain::09_AI

START
Cloze
In modern AI applications, text is represented as vectors called {embeddings} and used for {semantic search}, {caching}, and {Retrieval-Augmented Generation (RAG)}.
END

START
Cloze
In RAG, raw text is transformed into numerical representations called {embeddings} by an {embedding model}.
END

START
Cloze
Vector search, using tools like {FAISS} or {ChromaDB}, is analogous to a {Database Index} in Data Engineering, optimized for {distance metrics} instead of primary keys.
END

START
Cloze
A {Semantic Cache} in AI is similar to a {Materialized View} or {TTL-based Cache (Redis)} in Data Engineering, reusing previous answers for similar queries.
END

START
Cloze
The {RAG Pipeline} combines the user query with {retrieved documents} into an {Augmented Prompt} for the LLM.
END

START
Cloze
In the RAG context, the Augmented Prompt step is equivalent to a {Data Enrichment (Lookup) step} in an ETL pipeline.
END

START
Cloze
{Cosine Similarity} is used to measure the {semantic distance} between two blocks of text.
END

START
Cloze
{Embeddings} are high-dimensional {numerical arrays} that transform text.
END

START
Cloze
{FAISS} is a library for {high-performance similarity search}.
END

START
Cloze
{ChromaDB} is used for {local vector storage}.
END
