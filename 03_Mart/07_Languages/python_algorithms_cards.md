TARGET DECK: SecondBrain::07_Languages

START
Cloze
The **{Two Pointers}** pattern uses indices `left` and `right` moving towards each other to solve array problems in O(n) time and O(1) space.
END

START
Cloze
In the Sliding Window pattern, we maintain a {subset of elements} (window) that satisfies a specific condition as we iterate.
END

START
Cloze
The function `{isalnum()}` checks if a character is alphanumeric (either a letter or a number).
END

START
Cloze
For "Best Time to Buy and Sell Stock", the key insight is to track the **{minimum price seen so far}** and update max profit if current price - min price > max profit.
END

START
Cloze
Using a **{HashSet}** trades O(n) space for O(1) lookup time to detect duplicates.
END
